name: Stale Issues

on:
  schedule:
    - cron: '0 0 * * *'  # Daily at midnight UTC
  workflow_dispatch:

permissions:
  read-all

jobs:
  stale:
    permissions:
      issues: write
    uses: kubestellar/infra/.github/workflows/reusable-stale.yml@main
    secrets: inherit
