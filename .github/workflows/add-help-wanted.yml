name: Add Help Wanted Label

on:
  issues:
    types: [labeled]

permissions:
  read-all

jobs:
  label:
    permissions:
      issues: write
    uses: kubestellar/infra/.github/workflows/reusable-add-help-wanted.yml@main
    secrets: inherit
