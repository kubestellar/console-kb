name: Label Helper

on:
  issue_comment:
    types: [created]

permissions:
  read-all

jobs:
  label-helper:
    permissions:
      issues: write
      pull-requests: write
    uses: kubestellar/infra/.github/workflows/reusable-label-helper.yml@main
    secrets: inherit
