name: OpenSSF Scorecard

on:
  schedule:
    - cron: '0 6 * * 1'
  push:
    branches: [ "main" ]
  workflow_dispatch:

permissions:
  security-events: write
  contents: read
  actions: read

jobs:
  analysis:
    permissions:
      id-token: write
      security-events: write
      contents: read
      actions: read
    uses: kubestellar/infra/.github/workflows/reusable-scorecard.yml@220beeeb8dae67e2fd8e89338ada8144609fc6ef  # main
    secrets: inherit
