name: Copilot DCO

on:
  pull_request:
    types: [opened, synchronize, reopened]

permissions: read-all  # reusable workflow inherits its own permissions

jobs:
  copilot-dco:
    uses: kubestellar/infra/.github/workflows/reusable-copilot-dco.yml@a160acca0bdce1ac6c649e006d680d5f6d53024e  # main
