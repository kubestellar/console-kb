name: Assignment Helper

on:
  issue_comment:
    types: [created]

permissions:
  read-all

jobs:
  assignment-helper:
    permissions:
      issues: write
    uses: kubestellar/infra/.github/workflows/reusable-assignment-helper.yml@main
